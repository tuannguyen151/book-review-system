$("#marker-<%= @marker.status %>").html(`
  <%= j render "markers/form/cancel_#{@marker.status}" %>
`);

Swal.fire(
  "<%= I18n.t "js.markers.book_marker" %>",
  "<%= I18n.t "js.markers.marker", marker: status_marker(@marker) %>",
  "success"
);
