<%= form_with url: book_marker_path(@book, @marker), method: :delete do |f| %>
  <%= image_tag "svg/no-favorite.svg", class: "icon-marker" %>
  <%= f.button t(".cancel_favorite"), class: "btn btn-secondary rounded" %>
<% end %>
