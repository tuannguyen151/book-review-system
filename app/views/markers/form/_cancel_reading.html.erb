<%= form_with url: book_marker_path(@book, @marker), method: :delete do |f| %>
  <%= image_tag "svg/cancel-reading.svg", class: "icon-marker" %>
  <%= f.button t(".cancel_reading"), class: "btn btn-secondary rounded" %>
<% end %>
