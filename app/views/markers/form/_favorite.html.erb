<%= form_with url: book_markers_path(@book) do |f| %>
  <%= f.hidden_field :status, value: :favorite %>
  <%= image_tag "svg/favorite.svg", class: "icon-marker" %>
  <%= f.button t(".favorite"), class: "btn btn-primary rounded" %>
<% end %>
