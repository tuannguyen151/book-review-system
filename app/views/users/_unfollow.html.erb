<%= form_with url: user_relationship_path(user_id: @user, id: current_user),
  method: :delete do |f| %>
  <%= f.button t(".unfollow"), class: "btn btn-secondary btn-block rounded
    font-weight-bold text-light" %>
<% end %>
