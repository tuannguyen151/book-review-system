<% provide :title, t(".list_readed_books") %>
<% provide :icon, "icon/books.png"%>
<div class="site-section" id="main-content">
  <div class="container">
    <div class="row mb-5">
      <div class="col-md-7 text-left border-primary">
        <h2 class="font-weight-light text-primary">
          <%= t ".list_readed_books" %>
        </h2>
      </div>
    </div>
    <div class="col-md-12 mt-5" id="list-readed-books">
      <table id="table-readed-books"
        class="table table-striped table-bordered">
        <thead>
          <tr>
            <th><%= t ".cover_image" %></th>
            <th><%= t ".title" %></th>
            <th><%= t ".number_pages" %></th>
            <th><%= t ".author" %></th>
            <th><%= t ".price" %></th>
            <th><%= t ".action" %></th>
          </tr>
        </thead>
        <tbody>
          <%= render partial: "readed_books/readed_book",
            collection: @readed_books %>
        </tbody>
      </table>
    </div>
  </div>
</div>
